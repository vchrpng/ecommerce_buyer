import React from 'react';

const SizePickup = ({children}) => {
    return (
        <div>
            <h1>
            SizePickup</h1>
           <h1>{children}</h1>
            
        </div>

    )
}

export default SizePickup